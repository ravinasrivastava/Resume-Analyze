[project]
name = "careermatch-api"
version = "1.0.0"
description = "Explainable resume and job description matching API"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "python-multipart>=0.0.9",
    "pypdf>=5.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
